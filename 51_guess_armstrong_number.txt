#Find Armstrog number in a range
# count=0
# i=int(input("How many number do you want to check : "))
# for count in range(i):

start=int(input("Enter a start range : "))
end=int(input("Enter a end range : "))
armstrong_number=[]
for num in range(start,end):
    power=len(str(num))
    total=sum(int(digit)**power for digit in str(num))
    if num==total:
        armstrong_number.append(num)
print(f"Armstrong numbers: {armstrong_number}")
    
    
    
    #else:
    #print(f"{num} is not an  Armstrong number.")
    
#num=int(input("Guess an Armstrong number : "))    
    
    #count=count+1